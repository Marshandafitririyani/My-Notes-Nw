package com.example.mynotes.const

class Const {
    object USER {
        const val EMAIL = "email"
        const val PASSWORD = "password"
        const val PROFILE = "profile"
    }
    object TOKEN {
        const val API_TOKEN = "api_token"
        const val FCM_TOKEN = "fcm_token"
    }
    object NOTE{
        const val NOTE = "note"
    }
}